import query from './query.js';
import {
    factions,
    types,
    classes,
    partTypes,
    parts,
    lifeStages,
} from './parts.js';
import { utils } from 'ethers';
import { AsciiTable3, AlignmentEnum } from 'ascii-table3';

export default async function breed(graphClient) {

}